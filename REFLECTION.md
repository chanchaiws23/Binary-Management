ส่วนที่ยากที่สุดคือการจัดการ JWT ให้ frontend และ backend ทำงานสอดคล้องกัน
ตอนแรกเมื่อลบ token ผ่าน DevTools หน้ายังเปิดอยู่ เพราะ auth guard ตรวจแค่ตอน mount
จึงต้องเพิ่มการตรวจ token ซ้ำและจัดการ response 401 ให้กลับหน้า login อย่างถูกต้อง
อีกปัญหาคือการตั้งค่า PostgreSQL ผ่าน Docker, Prisma migration และ seed ให้เริ่มใหม่ได้จริง
และยังต้องปรับ validation จาก modal ที่รบกวนผู้ใช้ให้เป็นข้อความใต้ช่องที่กรอกผิด
ผมใช้ AI coding agent ช่วยวางโครง เขียนโค้ดบางส่วน ไล่หาสาเหตุ และรันการทดสอบ
ผมเป็นคนทดสอบ flow จริง ตรวจ UI และให้ agent แก้ทีละจุดพร้อมแยก commit
งานนี้ทำให้เข้าใจว่าต้องตรวจพฤติกรรมจริง ไม่ใช่ดูแค่ว่าโค้ด build ผ่าน
